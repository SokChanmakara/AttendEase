# AttendEase Mobile

Flutter employee app for attendance, QR login, and profile flows.

## Run

```bash
flutter run
```

Run commands from `packages/mobile`.

## Quality checks

```bash
flutter analyze
flutter test
```

## Notes

- Platform folders live in this package, not at the repo root.
- Mobile bundle/application identifier is `com.attendease.mobile`.
